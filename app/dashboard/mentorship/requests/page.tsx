"use client";
import{useEffect,useState}from"react";
type RequestRow={id:string;message:string|null;status:string;createdAt:string;mentee:{id:string;name:string};mentor:{id:string;name:string}};
export default function RequestsPage(){const[items,setItems]=useState<RequestRow[]>([]);useEffect(()=>{fetch("/api/mentorship/requests",{cache:"no-store"}).then(r=>r.json()).then(d=>setItems(d.requests??[]));},[]);return <div className="space-y-6"><section className="rounded-[32px] bg-black p-7 text-white md:p-9"><p className="text-[9px] uppercase tracking-[.2em] text-blue-300">Mentorship</p><h1 className="mt-3 text-3xl font-semibold md:text-5xl">Requests</h1><p className="mt-3 text-sm text-white/40">Review requests you sent or received.</p></section><section className="space-y-3">{items.map(r=><article key={r.id} className="rounded-[24px] border border-white/80 bg-white/60 p-5 backdrop-blur-xl"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-semibold">{r.mentee.name} → {r.mentor.name}</p><p className="mt-1 text-[9px] text-black/30">{new Date(r.createdAt).toLocaleString()}</p></div><span className="rounded-full bg-blue-500/10 px-3 py-1.5 text-[8px] text-blue-600">{r.status}</span></div>{r.message&&<p className="mt-4 rounded-[15px] bg-black/[.03] p-3 text-[10px] text-black/40">{r.message}</p>}</article>)}{items.length===0&&<div className="rounded-[24px] border border-dashed border-black/10 p-10 text-center text-[10px] text-black/30">No mentorship requests yet.</div>}</section></div>}