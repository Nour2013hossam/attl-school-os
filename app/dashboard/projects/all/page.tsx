"use client";
import Link from "next/link";
import {useEffect,useState} from "react";
type Project={id:string;title:string;description:string|null;status:string;visibility:string;progress:number;owner:{id:string;name:string};_count:{members:number;tasks:number}};
export default function ProjectsPage(){const [projects,setProjects]=useState<Project[]>([]);useEffect(()=>{fetch("/api/projects",{cache:"no-store"}).then(r=>r.json()).then(d=>setProjects(d.projects??[]));},[]);return <div className="space-y-6"><section className="rounded-[34px] border border-white/80 bg-white/65 p-7 shadow-[0_25px_80px_rgba(20,30,50,.08)] backdrop-blur-[30px]"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-[10px] font-semibold uppercase tracking-[.2em] text-blue-500">Project Lab</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.05em]">Projects</h1><p className="mt-3 text-sm text-black/40">Live projects from the School OS database.</p></div><Link href="/dashboard/projects/create" className="rounded-[16px] bg-black px-5 py-3 text-xs font-medium text-white shadow-lg">+ New Project</Link></div></section><section className="grid gap-4 lg:grid-cols-3">{projects.map(p=><article key={p.id} className="rounded-[28px] border border-white/80 bg-white/60 p-5 backdrop-blur-xl transition hover:-translate-y-1"><div className="flex items-start justify-between"><div className="flex h-11 w-11 items-center justify-center rounded-[15px] bg-black text-white">✦</div><span className="rounded-full bg-black/[.045] px-3 py-1 text-[9px] text-black/40">{p.status}</span></div><h2 className="mt-6 text-base font-semibold">{p.title}</h2><p className="mt-2 line-clamp-2 text-[10px] leading-5 text-black/35">{p.description??"No description provided."}</p><div className="mt-5 flex justify-between text-[9px] text-black/30"><span>{p.owner.name}</span><span>{p._count.members} members · {p._count.tasks} tasks</span></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-black/[.05]"><div className="h-full rounded-full bg-black" style={{width:Math.max(0,Math.min(100,p.progress))+"%"}}/></div><div className="mt-2 text-right text-[8px] text-black/30">{p.progress}% complete</div><Link href={"/dashboard/projects/my-projects?project="+p.id} className="mt-5 block text-[10px] text-blue-600">Open workspace →</Link></article>)}{projects.length===0&&<div className="rounded-[28px] border border-dashed border-black/10 p-10 text-center text-[10px] text-black/30 lg:col-span-3">No projects yet. Create the first one.</div>}</section></div>}