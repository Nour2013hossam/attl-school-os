"use client";
import{useEffect,useState}from"react";
type Event={id:string;title:string;description:string|null;startsAt:string;endsAt:string;location:string|null;capacity:number|null;_count?:{registrations:number}};
export default function EventsDiscoverPage(){const[items,setItems]=useState<Event[]>([]);const[busy,setBusy]=useState("");const[msg,setMsg]=useState("");async function load(){const r=await fetch("/api/events",{cache:"no-store"});const d=await r.json();setItems(d.events??[]);}useEffect(()=>{load();},[]);async function register(id:string){setBusy(id);const r=await fetch("/api/events/"+id+"/register",{method:"POST"});const d=await r.json();setMsg(r.ok?"Registered for event.":d.error??"Could not register.");setBusy("");if(r.ok)load();}return <div className="space-y-6"><section className="relative overflow-hidden rounded-[34px] border border-white/80 bg-white/65 p-7 backdrop-blur-[30px]"><div className="pointer-events-none absolute -right-24 -top-24 h-72 w-72 rounded-full bg-blue-400/15 blur-[90px]"/><p className="relative text-[10px] font-semibold uppercase tracking-[.2em] text-blue-500">Events</p><h1 className="relative mt-2 text-3xl font-semibold md:text-5xl">What's happening?</h1><p className="relative mt-3 text-sm text-black/40">Discover workshops, competitions, showcases and school activities.</p></section>{msg&&<div className="rounded-[15px] bg-blue-500/10 px-4 py-3 text-[9px] text-blue-700">{msg}</div>}<section className="grid gap-4 md:grid-cols-2">{items.map(e=><article key={e.id} className="group rounded-[28px] border border-white/80 bg-white/60 p-6 backdrop-blur-xl transition hover:-translate-y-1 hover:bg-white/80"><div className="flex items-start justify-between gap-3"><div className="rounded-[15px] bg-black px-4 py-3 text-center text-white"><p className="text-[8px] text-white/40">DATE</p><p className="mt-1 text-sm font-semibold">{new Date(e.startsAt).toLocaleDateString(undefined,{month:"short",day:"numeric"})}</p></div><span className="rounded-full bg-blue-500/10 px-3 py-1 text-[9px] text-blue-500">{e.location?"On-site":"Online"}</span></div><h2 className="mt-6 text-base font-semibold">{e.title}</h2><p className="mt-2 line-clamp-3 text-[10px] leading-5 text-black/40">{e.description??"School event."}</p><p className="mt-4 text-[9px] text-black/30">{new Date(e.startsAt).toLocaleString()} · {e.location??"Online"}</p><p className="mt-1 text-[8px] text-black/25">{e.capacity?((e._count?.registrations??0)+" / "+e.capacity+" registered"):((e._count?.registrations??0)+" registered")}</p><button disabled={busy===e.id} onClick={()=>register(e.id)} className="mt-6 w-full rounded-[14px] bg-black py-3 text-[9px] font-semibold text-white disabled:opacity-40">{busy===e.id?"Registering...":"Register for event"}</button></article>)}{items.length===0&&<div className="rounded-[24px] border border-dashed border-black/10 p-10 text-center text-[10px] text-black/30 md:col-span-2">No events have been published yet.</div>}</section></div>}