"use client";
import{useEffect,useState}from"react";
type Health={ok:boolean;service:string;database:string;timestamp?:string};
export default function SystemPage(){const[h,setH]=useState<Health|null>(null);const[t,setT]=useState<string[]>([]);
 async function load(){const r=await fetch("/api/health",{cache:"no-store"});const d=await r.json();setH(d);const p=await fetch("/api/admin/system/stats",{cache:"no-store"});if(p.ok){const x=await p.json();setT(x.stats??[]);}}
 useEffect(()=>{load();},[]);
 return <div className="space-y-6"><section className="rounded-[32px] bg-black p-7 text-white md:p-9"><p className="text-[9px] uppercase tracking-[.2em] text-blue-300">Admin OS</p><h1 className="mt-3 text-3xl font-semibold md:text-5xl">System</h1><p className="mt-3 text-sm text-white/40">Backend health, data footprint and platform signals.</p></section><section className="grid gap-3 md:grid-cols-3"><div className="rounded-[25px] border border-white/80 bg-white/60 p-5 backdrop-blur-xl"><p className="text-[8px] uppercase tracking-[.15em] text-black/25">Database</p><p className={"mt-2 text-lg font-semibold "+(h?.ok?"text-green-600":"text-red-600")}>{h?.database??"Checking..."}</p><p className="mt-1 text-[8px] text-black/25">{h?.timestamp?new Date(h.timestamp).toLocaleString():""}</p></div>{t.map((x,i)=><div key={i} className="rounded-[25px] border border-white/80 bg-white/60 p-5 backdrop-blur-xl"><p className="text-[8px] uppercase tracking-[.15em] text-black/25">{x.split(":")[0]}</p><p className="mt-2 text-lg font-semibold">{x.split(":").slice(1).join(":")}</p></div>)}</section><section className="rounded-[28px] border border-white/80 bg-white/60 p-6 backdrop-blur-xl"><p className="text-[8px] uppercase tracking-[.15em] text-black/25">Service</p><h2 className="mt-2 text-xl font-semibold">{h?.service??"ATTL School OS"}</h2><p className="mt-2 text-[10px] text-black/35">Use this page as the admin operational health center.</p><button onClick={load} className="mt-5 rounded-[14px] bg-black px-5 py-2.5 text-[9px] font-semibold text-white">Refresh</button></section></div>;
}