"use client";
import{useEffect,useState}from"react";
type Payload={roles:string[];matrix:Record<string,string[]>};
export default function PermissionsPage(){const[data,setData]=useState<Payload|null>(null);useEffect(()=>{fetch("/api/admin/roles",{cache:"no-store"}).then(r=>r.json()).then(setData);},[]);return <div className="space-y-6"><section className="rounded-[32px] bg-black p-7 text-white md:p-9"><p className="text-[9px] uppercase tracking-[.2em] text-blue-300">Admin OS</p><h1 className="mt-3 text-3xl font-semibold tracking-[-.05em] md:text-5xl">Permissions</h1><p className="mt-3 max-w-2xl text-sm text-white/40">The active authorization matrix for School OS roles.</p></section><section className="overflow-x-auto rounded-[28px] border border-white/80 bg-white/60 p-5 backdrop-blur-xl"><table className="min-w-[900px] w-full text-left"><thead><tr className="border-b border-black/5 text-[8px] uppercase tracking-[.15em] text-black/25"><th className="px-3 py-3">Role</th><th className="px-3 py-3">Permissions</th></tr></thead><tbody>{(data?.roles??[]).map(role=><tr key={role} className="border-b border-black/[.04] align-top text-[9px]"><td className="px-3 py-4 font-semibold">{role}</td><td className="px-3 py-4"><div className="flex flex-wrap gap-2">{(data?.matrix?.[role]??[]).map(p=><span key={p} className="rounded-full bg-black/[.04] px-2.5 py-1">{p}</span>)}</div></td></tr>)}</tbody></table></section></div>
}