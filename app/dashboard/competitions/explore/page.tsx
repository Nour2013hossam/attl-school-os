"use client";
import{useEffect,useState}from"react";
type Competition={id:string;title:string;description:string|null;organizer:string|null;startsAt:string|null;deadlineAt:string|null;location:string|null};
export default function CompetitionsPage(){const[items,setItems]=useState<Competition[]>([]);const[busy,setBusy]=useState("");const[msg,setMsg]=useState("");async function load(){const r=await fetch("/api/competitions",{cache:"no-store"});const d=await r.json();setItems(d.competitions??[]);}useEffect(()=>{load();},[]);async function apply(id:string){setBusy(id);const r=await fetch("/api/competitions/"+id+"/apply",{method:"POST"});const d=await r.json();setMsg(r.ok?"Application submitted.":d.error??"Could not apply.");setBusy("");}return <div className="space-y-6"><section className="relative overflow-hidden rounded-[34px] border border-white/80 bg-white/65 p-7 shadow-[0_25px_80px_rgba(20,30,50,.08)] backdrop-blur-[30px]"><div className="pointer-events-none absolute -right-20 -top-20 h-72 w-72 rounded-full bg-blue-400/15 blur-[90px]"/><div className="relative"><p className="text-[10px] font-semibold uppercase tracking-[.2em] text-blue-500">Competition Hub</p><h1 className="mt-2 text-3xl font-semibold md:text-5xl">Compete. Build. Represent.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-black/40">Discover competitions, challenges and opportunities to represent yourself and your school.</p></div></section>{msg&&<div className="rounded-[15px] bg-blue-500/10 px-4 py-3 text-[9px] text-blue-700">{msg}</div>}<section className="grid gap-4 lg:grid-cols-3">{items.map(c=><article key={c.id} className="group rounded-[28px] border border-white/80 bg-white/60 p-5 backdrop-blur-xl transition hover:-translate-y-1 hover:bg-white/85"><div className="flex items-start justify-between"><div className="flex h-12 w-12 items-center justify-center rounded-[16px] bg-black text-white">◇</div><span className="rounded-full bg-black/[.045] px-3 py-1 text-[9px] text-black/45">{c.deadlineAt&&new Date(c.deadlineAt)<new Date()?"Closed":"Open"}</span></div><p className="mt-6 text-[9px] uppercase tracking-wider text-blue-500">{c.organizer??"School"}</p><h2 className="mt-2 text-base font-semibold leading-6">{c.title}</h2><p className="mt-3 line-clamp-3 text-[10px] leading-5 text-black/40">{c.description??"Competition opportunity available through School OS."}</p><p className="mt-4 text-[9px] text-black/30">Deadline · {c.deadlineAt?new Date(c.deadlineAt).toLocaleString():"None"}</p><button disabled={busy===c.id||Boolean(c.deadlineAt&&new Date(c.deadlineAt)<new Date())} onClick={()=>apply(c.id)} className="mt-6 w-full rounded-[14px] bg-black py-3 text-[10px] text-white disabled:opacity-40">{busy===c.id?"Applying...":"Apply now"}</button></article>)}{items.length===0&&<div className="rounded-[24px] border border-dashed border-black/10 p-10 text-center text-[10px] text-black/30 lg:col-span-3">No competitions are listed yet.</div>}</section></div>